# Reproducible validation and coverage tooling. These Python packages are used
# only by repository checks; the Go runtime has a pinned, CGO-free SQLite module
# graph recorded in go.mod, go.sum, and third_party/go-modules.lock.json.
attrs==26.1.0
coverage==7.15.2
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
PyYAML==6.0.3
referencing==0.37.0
rpds-py==2026.6.3
typing_extensions==4.16.0
